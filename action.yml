name: 'RollHook Deploy'
description: 'Zero-secret zero-downtime deployment via RollHook — build, push, and deploy in one step using GitHub OIDC'
branding:
  icon: 'upload-cloud'
  color: 'blue'
inputs:
  url:
    description: 'RollHook server base URL (e.g. https://deploy.example.com)'
    required: true
  image_name:
    description: 'Image name (without registry prefix or tag), e.g. myapp'
    required: true
  image_tag:
    description: 'Pre-built image tag to deploy (skips build+push). Use when pushing to an external registry like GHCR.'
    required: false
  dockerfile:
    description: 'Path to Dockerfile'
    required: false
    default: 'Dockerfile'
  context:
    description: 'Docker build context path'
    required: false
    default: '.'
  build_args:
    description: 'Multi-line KEY=VALUE pairs forwarded as --build-arg to docker build. Values bake into the image — only use for non-secret build inputs like public API URLs or browser SDK ingestion tokens.'
    required: false
  timeout:
    description: 'Max seconds to wait for deployment to complete'
    required: false
    default: '600'
outputs:
  job_id:
    description: 'RollHook job ID'
  status:
    description: 'Final deployment status (success or failed)'
runs:
  using: 'node24'
  main: 'dist/index.js'
